"use client";

/**
 * Apps Layout
 *
 * This layout provides a dedicated space for integrated apps/modules.
 * It sits between the main dashboard layout and the individual module routes.
 */

export default function AppsLayout({ children }: { children: React.ReactNode }) {
	return <>{children}</>;
}
