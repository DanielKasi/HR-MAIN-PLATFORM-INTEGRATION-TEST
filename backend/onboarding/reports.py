from .models import (
    TerminationStage,
    TerminationType,
    Offboarding
)

REPORT_CONFIG = {
    'Termination Stagse': TerminationStage,
    'Termination Types' : TerminationType,
    'Offboarding': Offboarding
}

